# Optional: use a BYOK model profile without storing this key in learner state.
PYTHON_TUTOR_API_KEY=your_api_key

# Optional: local OpenAI-compatible endpoint (for example, Ollama).
PYTHON_TUTOR_BASE_URL=http://localhost:11434/v1
